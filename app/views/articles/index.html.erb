<div class="w-50 mx-auto mt-3">
  <% @articles.each do |article| %>
    <h2><%= link_to article.title, article_path(article), class: "link-article" %></h2>
    <p><%= article.content[0..50] %>...</p>
  <% end %>
</div>